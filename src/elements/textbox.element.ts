import MyElement from "../baseClassess/element.js";

export default class TextBox extends MyElement {
    constructor(name: string, elementLocaor: string) {
        super(name, elementLocaor);
    }
}